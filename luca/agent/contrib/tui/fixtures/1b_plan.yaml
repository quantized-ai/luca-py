# 1b — Multi-step plan / todo tracking, mid-run (composer queues).
#
# The plan is the DOCKED PANEL between the transcript and the composer, not a
# transcript block: it outlives the turn that wrote it, and mid-run it leads
# with the items the last write moved, struck through.
name: 1b_plan
status:
  cwd: ~/quantized/luca
  model: sonnet-4.5
  branch: main
  dirty: true
  tokens: 31.7k
  cost: $0.21
transcript:
  - kind: user
    text: migrate the event store to sqlite and keep the jsonl reader for old sessions
  - kind: text
    text: Four steps. I'll keep the jsonl reader behind a version check.
  - kind: tool
    tool: write
    arg: luca/store/schema.sql
    result:
      stat: {add: 34}
  - kind: tool
    tool: bash
    arg: pytest tests/test_store.py -q
    result:
      summary: "[success]6 passed[/] in 0.41s"
  - kind: thinking
    activity: reading luca/store/reader.py
plan:
  label: 4 tasks (2 done, 2 open)
  rows:
    - {glyph: done, text: "[faint]#1 -[/] define the sqlite schema in luca/store/schema.sql", strike: true}
    - {glyph: done, text: "[faint]#2 -[/] write the writer and its round-trip test", strike: true}
    - {glyph: active, text: "[faint]#3 -[/] port the reader, version-gated on session.format"}
    - {glyph: pending, text: "[faint]#4 -[/] backfill script + drop the jsonl writer"}
composer:
  placeholder: working…
  disabled: true
hints: [enter queue, ^p palette, esc interrupt]

We support a Storybook-like catalog of the main components of the UI. You can launch it with:

```bash
uv run textual run tests.agent.contrib.tui.previews.catalog
```

Make sure that any new component you implement is added to the catalog.
